 <%= form_for :message, url: [:reply, conversation] do |f| %>
   <%= f.text_area :body, placeholder: "Reply to this thread here..." %><br>
   <%= submit_tag 'Clear Reply', type: :reset, class: 'button-clear-message' %>
   <%= f.submit "Send Message", class: 'button-send-message' %>
 <% end %>