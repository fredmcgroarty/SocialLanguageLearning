<div class="profile col-md-3">
<%= render :partial => 'contact_user' %>
</div>
	<div class='profile-info'>
		<%= image_tag @user.picture.url(:medium) %>
		<br>
			<% if @user.user_profile %>
  		<% if @user.first_name && @user.last_name %>
	  	<dt>Name:</dt> <dd><%= @user.first_name + " " + @user.last_name %></dd>
	  	<% end %>
	  	<dt>Location:</dt> <dd>London</dd> 
	  	<% if @user.native_lang %>
	  	<dt>Native Language:</dt> <dd><%= @user.native_lang %></dd> 
	  	<% end %> 
	  	<% if @user.first_lang %>
	  	<dt>Looking To Learn:</dt> <dd><%= @user.first_lang %></dd>
	  	<% end %>
	  	<% if @user.email %>
	  	<dt>Email:</dt> <dd><%= @user.email %></dd> 
	  	<% end %>
		<% end %>
	</div>
</div>
<div class="col-md-3">
	<div id="completed-topics">
			<% if @completed_topics.any? %>
		<p class='discussed-topics'> <%= @user.first_name %> has discussed the following topics:</p>
				<% @completed_topics.each do |x| %> <br />
					<%= simple_format (x), class: 'profile list-group-item' %>
				<% end %>
				<% else %>
				None!<br />
			<% end %>
		Average Score: <%= @average_score %>
	</div>
</div>
<div class="col-md-3">
	<div id="calendar">
	</div>
</div>






	